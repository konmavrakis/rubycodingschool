<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @list.name %>
</p>

<p>
  <strong>Active:</strong>
  <%= @list.active %>
</p>

<p>
  <strong>Total price:</strong>
  <%= @total_price %>
</p>

<p>
  <strong>Products:</strong>
  <table>
    <tbody>
    <% @sku.each do |sku| %>
      <tr>
        <td>
          <img src="<%= sku.images['main'] %>" width = "30%" height = "30%" alt="<%= sku.name %>">
        </td>
        <td><%= sku.id %></td>
        <td><%= sku.name %></td>
      </tr>
    <% end %>
  </tbody>
  </table>

<%= link_to 'Edit', edit_list_path(@list) %> |
<%= link_to 'Back', lists_path %>
