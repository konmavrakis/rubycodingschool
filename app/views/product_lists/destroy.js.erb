$('#product-list-id-<%= @product_list.id %>').remove();

$('#final-price').text(function(){
  var sum = 0;
  $(".product-total-price").each(function(){
    sum += Number($(this).text());
  });
  return sum;
});

